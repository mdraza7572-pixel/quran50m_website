<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quran 50M - Daily Wazifa & Dua</title>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        :root {
            --bg-color: #050505;
            --glass-bg: rgba(255, 255, 255, 0.03);
            --glass-border: rgba(255, 255, 255, 0.08);
            --primary-gold: #FFD700;
            --accent-gradient: linear-gradient(135deg, #FFD700 0%, #FDB931 100%);
            --text-white: #ffffff;
            --text-gray: #a1a1aa;
            --btn-radius: 50px;
            --card-radius: 24px;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Outfit', sans-serif; scroll-behavior: smooth; }

        body {
            background-color: var(--bg-color); color: var(--text-white); line-height: 1.6;
            background-image: radial-gradient(circle at 15% 50%, rgba(251, 191, 36, 0.05) 0%, transparent 25%),
                              radial-gradient(circle at 85% 30%, rgba(20, 20, 40, 0.5) 0%, transparent 25%);
        }

        a { text-decoration: none; color: inherit; transition: 0.3s; }
        .container { max-width: 1200px; margin: 0 auto; padding: 0 20px; }
        .gradient-text { background: var(--accent-gradient); -webkit-background-clip: text; -webkit-text-fill-color: transparent; font-weight: 800; }

        /* Buttons */
        .btn { padding: 14px 32px; border-radius: var(--btn-radius); font-weight: 600; display: inline-flex; align-items: center; gap: 10px; transition: 0.3s; border: none; cursor: pointer; }
        .btn:hover { transform: translateY(-3px); }
        .btn-primary { background: var(--accent-gradient); color: #000; font-weight: 700; }
        .btn-red { background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%); color: white; }
        .btn-glass { background: rgba(255, 255, 255, 0.05); border: 1px solid var(--glass-border); color: white; }
        
        /* Header */
        header { display: flex; justify-content: space-between; align-items: center; padding: 15px 30px; margin-top: 20px; background: rgba(10, 10, 10, 0.6); backdrop-filter: blur(12px); border: 1px solid var(--glass-border); border-radius: 100px; position: sticky; top: 20px; z-index: 100; }
        .logo { font-size: 1.4rem; font-weight: 700; display: flex; align-items: center; gap: 12px; }
        .logo img { height: 45px; width: 45px; border-radius: 50%; border: 2px solid var(--primary-gold); }
        
        /* Hero */
        .hero { text-align: center; padding: 100px 20px 60px; }
        .badge { background: rgba(251, 191, 36, 0.08); color: var(--primary-gold); padding: 8px 20px; border-radius: 50px; font-size: 0.9rem; border: 1px solid rgba(251, 191, 36, 0.2); display: inline-flex; align-items: center; gap: 8px; margin-bottom: 25px; }
        .hero h1 { font-size: 3.5rem; margin-bottom: 20px; font-weight: 800; }
        .hero p { color: var(--text-gray); margin-bottom: 30px; font-size: 1.15rem; max-width: 700px; margin: 0 auto 30px auto; }

        /* Grids */
        .stats-grid, .benefits-grid { display: grid; gap: 20px; margin: 40px 0; }
        .stats-grid { grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); }
        .benefits-grid { grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); }
        
        .stat-card, .benefit-card { background: var(--glass-bg); border: 1px solid var(--glass-border); padding: 30px 20px; border-radius: var(--card-radius); text-align: center; }
        .benefit-card { background: linear-gradient(180deg, #0e0e0e 0%, #050505 100%); display: flex; align-items: center; gap: 15px; text-align: left; padding: 25px; }
        .benefit-icon { color: var(--primary-gold); font-size: 1.2rem; background: rgba(251, 191, 36, 0.1); padding: 12px; border-radius: 12px; }
        .stat-number { font-size: 2rem; font-weight: 800; color: white; }

        /* Video Section */
        .section-title { margin-bottom: 50px; text-align: center; margin-top: 80px; }
        .section-title h2 { font-size: 2.5rem; margin-bottom: 10px; font-weight: 800; }
        .video-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 30px; }
        
        .video-card { background: #0a0a0a; border-radius: var(--card-radius); overflow: hidden; border: 1px solid var(--glass-border); transition: 0.4s; display: block; }
        .video-card:hover { transform: translateY(-10px); border-color: #333; }
        .thumbnail { position: relative; height: 200px; overflow: hidden; }
        .thumbnail img { width: 100%; height: 100%; object-fit: cover; transition: 0.5s; }
        .video-card:hover .thumbnail img { transform: scale(1.05); }
        .play-icon { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); font-size: 1.5rem; color: black; background: var(--primary-gold); width: 55px; height: 55px; border-radius: 50%; display: flex; align-items: center; justify-content: center; }
        .video-info { padding: 25px; }
        .video-title { color: white; font-weight: 600; font-size: 1.1rem; margin-bottom: 5px; }
        .video-desc { color: var(--text-gray); font-size: 0.9rem; }

        /* Network & Contact */
        .network-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(350px, 1fr)); gap: 30px; margin-top: 40px; }
        .network-card { background: #0e0e0e; border: 1px solid var(--glass-border); border-radius: 30px; padding: 25px; display: flex; align-items: center; gap: 25px; color: white; text-decoration: none; }
        .network-logo { width: 90px; height: 90px; border-radius: 25px; object-fit: cover; border: 2px solid #222; }

        .support-box { background: linear-gradient(45deg, #111, #000); border: 1px solid var(--glass-border); padding: 40px; border-radius: 30px; text-align: center; margin: 80px 0; }
        .contact-wrapper { display: grid; grid-template-columns: 1fr; gap: 50px; margin-bottom: 80px; }
        @media(min-width: 768px) { .contact-wrapper { grid-template-columns: 1fr 1.2fr; } }
        .form-input { width: 100%; padding: 16px 25px; background: #050505; border: 2px solid #1a1a1a; border-radius: 18px; color: white; margin-bottom: 20px; }
        
        footer { border-top: 1px solid #1a1a1a; padding: 60px 20px; text-align: center; background: #000; color: #666; font-size: 0.85rem; }
    </style>
</head>
<body>

    <div class="container">
        <header>
            <div class="logo"><img src="logo.png" alt="Logo"> <span>Quran 50M</span></div>
            <a href="https://youtube.com/@quran50m" target="_blank" class="btn btn-red"><i class="fab fa-youtube"></i> Subscribe</a>
        </header>
    </div>

    <section class="hero container">
        <div class="badge"><i class="fas fa-moon"></i> Assalamu Alaikum Warahmatullahi Wabarakatuh</div>
        <h1>Daily Quran <br> <span class="gradient-text">Wazifa & Dua</span></h1>
        <p>Is video mein aap Holy Quran ki beautiful aur heart-touching tilawat sun sakte hain.</p>
        <div style="display: flex; gap: 15px; justify-content: center;">
            <a href="#playlist" class="btn btn-primary"><i class="fab fa-play"></i> Watch Playlist</a>
            <a href="#benefits" class="btn btn-glass">Read Benefits</a>
        </div>
        
        <div class="stats-grid">
            <div class="stat-card"><i class="fas fa-users" style="color:var(--primary-gold); font-size:1.5rem;"></i><div class="stat-number">50M+</div><div class="stat-label">Community</div></div>
            <div class="stat-card"><i class="fas fa-star" style="color:var(--primary-gold); font-size:1.5rem;"></i><div class="stat-number">Daily</div><div class="stat-label">Dua & Wazifa</div></div>
            <div class="stat-card"><i class="fas fa-heart" style="color:var(--primary-gold); font-size:1.5rem;"></i><div class="stat-number">Peace</div><div class="stat-label">Spiritual Healing</div></div>
        </div>
    </section>

    <section id="benefits" class="container">
        <div class="section-title"><h2>Why Listen <span class="gradient-text">Daily?</span></h2></div>
        <div class="benefits-grid">
            <div class="benefit-card"><div class="benefit-icon"><i class="fas fa-heart"></i></div><div><h4 style="color:white;">Dil Ka Sukoon</h4><p style="font-size:0.85rem; color:#888;">Dil ke bojh ko kam karta hai.</p></div></div>
            <div class="benefit-card"><div class="benefit-icon"><i class="fas fa-brain"></i></div><div><h4 style="color:white;">Stress Relief</h4><p style="font-size:0.85rem; color:#888;">Pareshani aur anxiety door.</p></div></div>
            <div class="benefit-card"><div class="benefit-icon"><i class="fas fa-shield-alt"></i></div><div><h4 style="color:white;">Protection</h4><p style="font-size:0.85rem; color:#888;">Roohani shifa aur hifazat.</p></div></div>
        </div>
    </section>

    <section id="playlist" class="container">
        <div class="section-title">
            <h2>Tilawat <span class="gradient-text">Playlist</span></h2>
            <p style="color: var(--text-gray);">Beautiful collection of Quran recitations</p>
        </div>
        <div class="video-grid" id="videoContainer">
            </div>
    </section>

    <section class="container network-section">
        <div class="section-title"><h2>Our <span class="gradient-text">Network</span></h2></div>
        <div class="network-grid">
            <a href="https://youtube.com/@quranindiatv335" target="_blank" class="network-card">
                <img src="quran-india.jpg" class="network-logo"><div class="network-info"><h3>Quran India TV</h3><p>Discover beautiful Quranic recitations.</p></div>
            </a>
            <a href="https://youtube.com/@khushbue786quran" target="_blank" class="network-card">
                <img src="khushbu.jpg" class="network-logo"><div class="network-info"><h3>Khushbu e Quran</h3><p>Fragrance of the Holy Quran.</p></div>
            </a>
        </div>
    </section>

    <section class="container contact-section">
        <div class="contact-wrapper">
            <div>
                <h2>Get in <span class="gradient-text">Touch</span></h2>
                <p style="color:var(--text-gray); margin-bottom: 20px;">We'd love to hear from you.</p>
                <div style="color:var(--text-gray); display:flex; align-items:center; gap:10px;"><i class="fas fa-envelope" style="color:var(--primary-gold)"></i> support@quran50m.online</div>
            </div>
            <form action="https://formsubmit.co/mdraza7572@gmail.com" method="POST">
                <input type="hidden" name="_captcha" value="false">
                <input type="text" name="name" class="form-input" placeholder="Your Name" required>
                <input type="email" name="email" class="form-input" placeholder="Your Email" required>
                <textarea name="message" class="form-input" rows="4" placeholder="Your Message..." required></textarea>
                <button type="submit" class="btn btn-primary" style="width: 100%; border-radius: 18px;">Send Message</button>
            </form>
        </div>
    </section>

    <footer>
        <div class="logo" style="justify-content: center; margin-bottom: 20px;"><img src="logo.png" alt="Logo"> <span>Quran 50M</span></div>
        <p>&copy; 2025 Quran 50M. All rights reserved.</p>
    </footer>

    <script src="playlist.js"></script>
    <script>
        const container = document.getElementById('videoContainer');
        
        // Function to extract video ID
        function getYouTubeId(url) {
            const regExp = /^.*(youtu.be\/|v\/|u\/\w\/|embed\/|watch\?v=|&v=)([^#&?]*).*/;
            const match = url.match(regExp);
            return (match && match[2].length === 11) ? match[2] : null;
        }

        if (typeof myVideos !== 'undefined' && myVideos.length > 0) {
            myVideos.forEach(video => {
                const videoId = getYouTubeId(video.link);
                if(videoId) {
                    const card = document.createElement('a');
                    card.href = video.link;
                    card.target = "_blank";
                    card.className = "video-card";
                    card.innerHTML = `
                        <div class="thumbnail">
                            <img src="https://img.youtube.com/vi/${videoId}/maxresdefault.jpg" alt="${video.title}">
                            <div class="play-icon"><i class="fas fa-play"></i></div>
                        </div>
                        <div class="video-info">
                            <div class="video-title">${video.title}</div>
                            <div class="video-desc">${video.desc}</div>
                        </div>
                    `;
                    container.appendChild(card);
                }
            });
        } else {
            container.innerHTML = '<p style="text-align:center; color:#888;">No videos found in playlist.</p>';
        }
    </script>
</body>
</html>
